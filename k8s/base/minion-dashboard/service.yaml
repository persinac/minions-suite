apiVersion: v1
kind: Service
metadata:
  name: minion-dashboard
  labels:
    app.kubernetes.io/name: minion-dashboard
    app.kubernetes.io/component: ui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: minion-dashboard
  ports:
    - name: http
      port: 8322
      targetPort: http
      protocol: TCP
