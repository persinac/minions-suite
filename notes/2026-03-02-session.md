# 2026-03-02 Session: Dashboard, Dependencies, Config Fixes

## What We Did

### 1. Ported Dashboard from mcp-minions

- **Created `minions/dashboard.py`** — FastAPI web UI showing jobs, tasks, agents, timeline, messages. Read-only, queries DB directly. HTMX auto-refresh every 5s. Runs on port 8322.
- **Created `minions/connectors/nats_subscriber.py`** — NATS JetStream async generator for the dashboard SSE endpoint. Maintains persistent subscription with reconnect logic.
- **Added `--dashboard` CLI flag** to `minions/cli.py` — wired into the main dispatch block, calls `run_dashboard()` from the new module.

### 2. Added Missing Dependencies

- **`pyproject.toml`** — added `fastapi>=0.135.1` and `uvicorn>=0.41.0` (user later pinned `fastmcp>=2.0,<3`, `nats-py>=2.12,<2.13`, added `kubernetes-asyncio>=31.0.0`)
- Ran `uv lock` to regenerate lockfile.

### 3. Fixed Postgres Config (`_build_postgres_url`)

- **Root cause:** minions-suite was reading `PG_HOST`, `PG_USER`, etc. but the Doppler secret has `DB_HOST`, `DB_ADMIN`, `DB_PASSWORD`, `DB_PORT`, `DB_NAME`.
- **Fix:** Updated `minions/config.py` `_build_postgres_url()` to read `DB_*` env vars matching the actual secret keys.

### 4. Rewrote Preflight Checks

- **Replaced `minions/preflight.py`** with a version aligned to mcp-minions:
  - Added `_in_container()` detection (checks `CONTAINER_ENV` or `/.dockerenv`)
  - Git provider check is now WARN (not FAIL) inside containers
  - Postgres check validates `minions` schema exists (not just connectivity)
  - Added arbiter prerequisite check
  - Prints "Minions Suite Preflight Checks" header

### 5. Infrastructure Changes

- **`mcp-minion-infrastructure/pulumi/tenants/mcpm-cackalacky.yaml`** — set `slim_image` to the ECR URL (was empty string). This enables the dashboard and trello-poller deployments via the existing `if slim_image:` gate in `component.py`.

### 6. Unified Review Schema (partial)

- **Changed `REVIEW_SCHEMA` from `"reviewer"` to `"minions"`** in `db_postgres.py` — the `reviewer` schema never existed in any migration.

## What's Left (see PLAN-unify-review-schema.md)

1. **Migration needed:** `reviews` and `review_comments` tables don't exist in the `minions` Postgres schema yet. Need `ALTER TABLE minions.agents ADD COLUMN review_id` and `ALTER COLUMN job_id DROP NOT NULL`.
2. **FastMCP API:** `cli.py:230` calls `mcp.run_sse_async()` which doesn't exist in FastMCP v2. Should be `mcp.run_async(transport="sse", ...)`.
3. **Simplify `db_postgres.py`:** Remove the dual-schema try-both-tables agent logic now that everything is in `minions`.

## Current State After Deploy

- Preflight passes (postgres connected, minions schema found, NATS connected, arbiter prerequisites met)
- Dashboard pod starts and serves on 8322
- Trello poller runs and polls board
- **MCP server crashes** on two issues:
  - `reviewer.reviews` table doesn't exist (review engine poll)
  - `run_sse_async` AttributeError (FastMCP v2 API change)
